

def compute_join_order_loss_pg_single(query, true_cardinalities,
        est_cardinalities):
    '''
    @query: str
    @true_cardinalities:
        key:
            sort([table_1 / alias_1, ..., table_n / alias_n])
        val:
            float
    @est_cardinalities:
        key:
            sort([table_1 / alias_1, ..., table_n / alias_n])
        val:
            float
    '''

    # set est cardinalities


    # find join order

    # set true cardinalities

    # find cost1, and cost2
